# covid-twitter-scraper

## Requirements

Requires docker with docker-compose

## Run

Run `docker-compose up` to start. File `tweets.db` will be mounted to current directory. It's sqlite database containing scraped tweets.